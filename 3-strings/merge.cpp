//
// Created by DELL on 2024/11/8.
//
#include <iostream>

using namespace std;

int main() {
  string s, t, r;
  cin >> s >> t >> r;

  int lens = s.size();
  int lent = t.size();
  int lenr = r.size();
  if (lens + lent != lenr) { puts("False"); return 0; }

  int idxs = 0, idxt = 0, idxr = 0;
  while (idxs < lens && idxt < lent) {

  }

  return 0;
}